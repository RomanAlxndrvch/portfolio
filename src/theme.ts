import {createTheme} from '@mui/material/styles';

export let theme = createTheme({
    palette: {
        primary: {
            main: '#0052cc',
        },
        secondary: {
            main: '#767676',
        },
    },
});